# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Flet build output
build/
dist/
storage/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Flet publish output
src/dist/
.flet/
src/.flet/

# Local wallet config (see flet-pay-example README)
src/local_config.toml

# Real credentials -- the Pinata key for tools/publish_ipfs.py. Kept out
# of src/ so `flet publish` cannot bundle it into the app.
local_secrets.toml

# uv lockfile - the venv is reproducible from requirements.txt
uv.lock

# Compiled from vendor/curve-assets by tools/build_assets.py
src/assets/curve/
